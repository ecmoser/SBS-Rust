[package]
name = "sbs-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.15.0"
reqwest = { version = "0.13.4", features = ["blocking"] }
scraper = "0.27.0"
serde = { version = "1.0.228", features = ["derive"] }
